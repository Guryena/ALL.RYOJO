<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.global.ex.mapper.BoardMapper"> <!--interface name -->


	<select id="getList"
		resultType="com.Allryojo.arj.vo.PopulationVO">
  				
     <![CDATA[
      select * from mvc_board order by bgroup desc, bstep asc
   ]]>
	</select>

	<insert id="insert">
  				
     <![CDATA[
       insert into mvc_board (bid, bname, btitle, bcontent, bhit, bgroup, bstep, bindent) values (mvc_board_seq.nextval, #{bname}, #{btitle}, #{bcontent}, 0, mvc_board_seq.currval, 0, 0) 
   ]]>
		<!--파라미터를 남길때는 # -->
	</insert>

</mapper>